# Sponsor logos

Drop sponsor logo files (PNG or SVG, roughly 120×60) into this folder,
then reference them in `data/sponsors.json`:

```json
{ "name": "My Sponsor", "image": "assets/sponsors/my-sponsor.png" }
```

Entries with `"image": null` render as styled text placeholders.
